// adding image and handing it off to the next guy.
function  addImage(req, res, next) {

  console.log("LOGIT: request %S, %S", req.method, req.url);

  if(url.match())

  next();
}

module.export = addImage;
